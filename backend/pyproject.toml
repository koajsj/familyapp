[project]
name = "familyapp-backend"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115", "sqlalchemy>=2.0", "psycopg[binary]>=3.2", "pydantic>=2.9"]

[tool.ruff]
line-length = 100
