# Scaffold only. Do not put a real secret in this file or commit a populated .env.
DATABASE_URL=postgresql+psycopg://familyapp:change-me@localhost:5432/familyapp
JWT_SECRET=replace-with-a-long-random-secret-before-running
ENVIRONMENT=development
ACCESS_TOKEN_TTL_MINUTES=60
